# macOS
.DS_Store

# Source PDFs — sourced manually from PANW IR, not committed (re-source for each quarter)
demo/data/manual/*.pdf

# Large binary workshop files — store externally (Google Drive / Dropbox)
*.pptx
.AppleDouble
.LSOverride

# SQLite databases (rebuilt from raw data; do not commit generated artifacts)
*.db
*.db-journal
*.db-shm
*.db-wal

# Python
__pycache__/
*.py[cod]
*.pyo
.env
.venv/
venv/

# Editor
.vscode/
.idea/
